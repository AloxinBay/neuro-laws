# Зависимости для ЛОКАЛЬНОГО обучения (без bitsandbytes/trl).
# PyTorch ставится отдельно через Setup_train.bat (CPU или CUDA).
transformers==4.44.2
datasets==2.21.0
accelerate==0.33.0
sentencepiece
regex
